// Automatically generated by isl_bindings_generator.
// LICENSE: MIT

use libc::uintptr_t;
use std::os::raw::{c_void, c_char};
use crate::bindings::{Stat, Id, Context};
use std::ffi::{CString, CStr};

/// Wraps `isl_id_list`.
pub struct IdList {
    pub ptr: uintptr_t,
    pub should_free_on_drop: bool,
}

extern "C" {

    fn isl_id_list_clear(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_map(list: uintptr_t, fn_: unsafe extern "C" fn(uintptr_t, *mut c_void) -> uintptr_t, user: *mut c_void) -> uintptr_t;

    fn isl_id_list_free(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_drop(list: uintptr_t, first: u32, n: u32) -> uintptr_t;

    fn isl_id_list_foreach(list: uintptr_t, fn_: unsafe extern "C" fn(uintptr_t, *mut c_void) -> Stat, user: *mut c_void) -> Stat;

    fn isl_id_list_insert(list: uintptr_t, pos: u32, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_sort(list: uintptr_t, cmp: unsafe extern "C" fn(uintptr_t, uintptr_t, *mut c_void) -> i32, user: *mut c_void) -> uintptr_t;

    fn isl_id_list_foreach_scc(list: uintptr_t, follows: unsafe extern "C" fn(uintptr_t, uintptr_t, *mut c_void) -> i32, follows_user: *mut c_void, fn_: unsafe extern "C" fn(uintptr_t, *mut c_void) -> Stat, fn_user: *mut c_void) -> Stat;

    fn isl_id_list_reverse(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_every(list: uintptr_t, test: unsafe extern "C" fn(uintptr_t, *mut c_void) -> i32, user: *mut c_void) -> i32;

    fn isl_id_list_set_id(list: uintptr_t, index: i32, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_dump(list: uintptr_t);

    fn isl_id_list_add(list: uintptr_t, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_size(list: uintptr_t) -> i32;

    fn isl_id_list_concat(list1: uintptr_t, list2: uintptr_t) -> uintptr_t;

    fn isl_id_list_swap(list: uintptr_t, pos1: u32, pos2: u32) -> uintptr_t;

    fn isl_id_list_to_str(list: uintptr_t) -> *const c_char;

    fn isl_id_list_copy(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_get_at(list: uintptr_t, index: i32) -> uintptr_t;

    fn isl_id_list_read_from_str(ctx: uintptr_t, str_: *const c_char) -> uintptr_t;

    fn isl_id_list_from_id(el: uintptr_t) -> uintptr_t;

    fn isl_id_list_get_ctx(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_get_id(list: uintptr_t, index: i32) -> uintptr_t;

    fn isl_id_list_alloc(ctx: uintptr_t, n: i32) -> uintptr_t;

    fn isl_id_list_n_id(list: uintptr_t) -> i32;

}

impl Clone for IdList {
    fn clone(&self) -> IdList {
        self.copy()
    }
}

impl FromIterator<Id> for IdList {
    fn from_iter<T>(iter: T) -> Self
    where T: IntoIterator<Item = Id>,
    {
        let mut iter = iter.into_iter().peekable();
        let ctx = iter.peek().unwrap().get_ctx();
        let (size, _) = iter.size_hint();
        let mut res = Self::alloc(&ctx, size as _);
        for x in iter {
        res = res.add(x);
        }
        res
    }
}

impl IdList {
    /// Wraps `isl_id_list_clear`.
    pub fn clear(self) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_clear(list) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_map`.
    pub fn map<fn_>(self, mut fn_: fn_, user: *mut c_void) -> IdList
    where fn_: FnMut(&Id) -> &Id,
    {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        extern "C" fn fn__wrapper<fn_>(arg_0: uintptr_t, arg_1: *mut c_void) -> uintptr_t {
            let fn_: *mut fn_ = unsafe { core::mem::transmute(arg_1) };
            let fn_: &mut fn_ = unsafe { &mut *fn_ };
            todo!()
        }

        let user: *mut fn_ = &mut fn_;
        let user: *mut c_void = unsafe { core::mem::transmute(user) };
        let fn_ = fn__wrapper::<fn_>;
        let isl_rs_result = unsafe { isl_id_list_map(list, fn_, user) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_free`.
    pub fn free(self) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_free(list) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_drop`.
    pub fn drop(self, first: u32, n: u32) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_drop(list, first, n) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_foreach`.
    pub fn foreach<fn_>(&self, mut fn_: fn_, user: *mut c_void) -> Stat
    where fn_: FnMut(&Id) -> Stat,
    {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        extern "C" fn fn__wrapper<fn_>(arg_0: uintptr_t, arg_1: *mut c_void) -> Stat {
            let fn_: *mut fn_ = unsafe { core::mem::transmute(arg_1) };
            let fn_: &mut fn_ = unsafe { &mut *fn_ };
            todo!()
        }

        let user: *mut fn_ = &mut fn_;
        let user: *mut c_void = unsafe { core::mem::transmute(user) };
        let fn_ = fn__wrapper::<fn_>;
        let isl_rs_result = unsafe { isl_id_list_foreach(list, fn_, user) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_insert`.
    pub fn insert(self, pos: u32, el: Id) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_insert(list, pos, el) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_sort`.
    pub fn sort<cmp>(self, mut cmp: cmp, user: *mut c_void) -> IdList
    where cmp: FnMut(&Id, &Id) -> i32,
    {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        extern "C" fn cmp_wrapper<cmp>(arg_0: uintptr_t, arg_1: uintptr_t, arg_2: *mut c_void) -> i32 {
            let cmp: *mut cmp = unsafe { core::mem::transmute(arg_2) };
            let cmp: &mut cmp = unsafe { &mut *cmp };
            todo!()
        }

        let cmp_user: *mut cmp = &mut cmp;
        let cmp_user: *mut c_void = unsafe { core::mem::transmute(cmp_user) };
        let cmp = cmp_wrapper::<cmp>;
        let isl_rs_result = unsafe { isl_id_list_sort(list, cmp, user) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_foreach_scc`.
    pub fn foreach_scc<follows, fn_>(&self, mut follows: follows, follows_user: *mut c_void, mut fn_: fn_, fn_user: *mut c_void) -> Stat
    where follows: FnMut(&Id, &Id) -> bool,
          fn_: FnMut(&IdList) -> Stat,
    {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        extern "C" fn follows_wrapper<follows>(arg_0: uintptr_t, arg_1: uintptr_t, arg_2: *mut c_void) -> i32 {
            let follows: *mut follows = unsafe { core::mem::transmute(arg_2) };
            let follows: &mut follows = unsafe { &mut *follows };
            todo!()
        }

        let follows_user: *mut follows = &mut follows;
        let follows_user: *mut c_void = unsafe { core::mem::transmute(follows_user) };
        let follows = follows_wrapper::<follows>;
        extern "C" fn fn__wrapper<fn_>(arg_0: uintptr_t, arg_1: *mut c_void) -> Stat {
            let fn_: *mut fn_ = unsafe { core::mem::transmute(arg_1) };
            let fn_: &mut fn_ = unsafe { &mut *fn_ };
            todo!()
        }

        let user: *mut fn_ = &mut fn_;
        let user: *mut c_void = unsafe { core::mem::transmute(user) };
        let fn_ = fn__wrapper::<fn_>;
        let isl_rs_result = unsafe { isl_id_list_foreach_scc(list, follows, follows_user, fn_, fn_user) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_reverse`.
    pub fn reverse(self) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_reverse(list) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_every`.
    pub fn every<test>(&self, mut test: test, user: *mut c_void) -> bool
    where test: FnMut(&Id) -> bool,
    {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        extern "C" fn test_wrapper<test>(arg_0: uintptr_t, arg_1: *mut c_void) -> i32 {
            let test: *mut test = unsafe { core::mem::transmute(arg_1) };
            let test: &mut test = unsafe { &mut *test };
            todo!()
        }

        let test_user: *mut test = &mut test;
        let test_user: *mut c_void = unsafe { core::mem::transmute(test_user) };
        let test = test_wrapper::<test>;
        let isl_rs_result = unsafe { isl_id_list_every(list, test, user) };
        let isl_rs_result = match isl_rs_result {
            0 => false,
            1 => true,
            _ => panic!("ISL error: {}", context_for_error_message.last_error_msg()),
        };
        isl_rs_result
    }

    /// Wraps `isl_id_list_set_id`.
    pub fn set_id(self, index: i32, el: Id) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_set_id(list, index, el) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_dump`.
    pub fn dump(&self) {
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_dump(list) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_add`.
    pub fn add(self, el: Id) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_add(list, el) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_size`.
    pub fn size(&self) -> i32 {
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_size(list) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_concat`.
    pub fn concat(self, list2: IdList) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list1 = self;
        let mut list1 = list1;
        list1.do_not_free_on_drop();
        let list1 = list1.ptr;
        let mut list2 = list2;
        list2.do_not_free_on_drop();
        let list2 = list2.ptr;
        let isl_rs_result = unsafe { isl_id_list_concat(list1, list2) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_swap`.
    pub fn swap(self, pos1: u32, pos2: u32) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_swap(list, pos1, pos2) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_to_str`.
    pub fn to_str(&self) -> &str {
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_to_str(list) };
        let isl_rs_result = unsafe { CStr::from_ptr(isl_rs_result) };
        let isl_rs_result = isl_rs_result.to_str().unwrap();
        isl_rs_result
    }

    /// Wraps `isl_id_list_copy`.
    pub fn copy(&self) -> IdList {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_copy(list) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_at`.
    pub fn get_at(&self, index: i32) -> Id {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_at(list, index) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = Id { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_read_from_str`.
    pub fn read_from_str(ctx: &Context, str_: &str) -> IdList {
        let ctx = ctx.ptr;
        let str_ = CString::new(str_).unwrap();
        let str_ = str_.as_ptr();
        let isl_rs_result = unsafe { isl_id_list_read_from_str(ctx, str_) };
        if isl_rs_result == 0 {
            panic!("ISL error");
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_from_id`.
    pub fn from_id(el: Id) -> IdList {
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_from_id(el) };
        if isl_rs_result == 0 {
            panic!("ISL error");
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_ctx`.
    pub fn get_ctx(&self) -> Context {
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_ctx(list) };
        if isl_rs_result == 0 {
            panic!("ISL error");
        }
        let isl_rs_result = Context { ptr: isl_rs_result, should_free_on_drop: true };
        let mut isl_rs_result = isl_rs_result;
        isl_rs_result.do_not_free_on_drop();
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_id`.
    pub fn get_id(&self, index: i32) -> Id {
        let context_for_error_message = self.get_ctx();
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_id(list, index) };
        if isl_rs_result == 0 {
            panic!("ISL error: {}", context_for_error_message.last_error_msg());
        }
        let isl_rs_result = Id { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_alloc`.
    pub fn alloc(ctx: &Context, n: i32) -> IdList {
        let ctx = ctx.ptr;
        let isl_rs_result = unsafe { isl_id_list_alloc(ctx, n) };
        if isl_rs_result == 0 {
            panic!("ISL error");
        }
        let isl_rs_result = IdList { ptr: isl_rs_result, should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_n_id`.
    pub fn n_id(&self) -> i32 {
        let list = self;
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_n_id(list) };
        isl_rs_result
    }

    /// Does not call isl_xxx_free() on being dropped. (For internal use only.)
    pub fn do_not_free_on_drop(&mut self) {
        self.should_free_on_drop = false;
    }
}

impl Drop for IdList {
    fn drop(&mut self) {
        if self.should_free_on_drop {
            unsafe { isl_id_list_free(self.ptr); }
        }
    }
}